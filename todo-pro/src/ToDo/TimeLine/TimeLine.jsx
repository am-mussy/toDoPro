import React from "react";
import styled from "@emotion/styled";

const TimeLineWrapper = styled.div({
  backgroundColor: "gray",
  maxWidth: 300,
  height: "100vh",
  minWidth: 280,
});

function TimeLine() {
  return <TimeLineWrapper></TimeLineWrapper>;
}

export default TimeLine;
